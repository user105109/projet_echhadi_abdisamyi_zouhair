version: "3.8"

services:
  web:
    build: .
    ports:
      - "8080:3000"
    depends_on:
      - redis-server

  redis-server:
    image: redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data: